# Tempo de resposta de TODAS as requisições do mock (em SEGUNDOS).
# Copie este arquivo para .env e ajuste o valor conforme necessário.
# Usado pelo mocks.config.js — zere para respostas instantâneas.
MOCK_DELAY_SECONDS=2
